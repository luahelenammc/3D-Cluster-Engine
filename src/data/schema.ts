import graphDatasetSchemaJson from "../../schema/graph-dataset.schema.json";

/** The public JSON Schema is the single structural source of truth. */
export const graphDatasetSchema = graphDatasetSchemaJson;
